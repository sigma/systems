{ ... }:
{
  enable = true;

  settings = {
    # keybindings behaviors
    "enter_accept" = false;
    "filter_mode_shell_up_key_binding" = "directory";

    # sync
    "sync.records" = true;

    # style
    "style" = "compact";
    "inline_height" = 30;
  };
}
