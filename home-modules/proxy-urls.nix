# Shared API proxy URL constants for wrapper modules.
# Centralizes endpoint URLs so they aren't duplicated across
# claude-firefly.nix, opencode-firefly.nix, etc.
{
  tailscaleProxy = "http://ai.van-scylla.ts.net";
}
